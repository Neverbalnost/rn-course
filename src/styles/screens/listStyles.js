import { StyleSheet } from 'react-native';

export default listStyles = StyleSheet.create({
    item: {
        alignItems: 'center',
        flex: 1,
        flexDirection: 'row',
        marginVertical: 10,
    },
    icon: {
        marginRight: 10
    },
    link: {
        marginLeft: 'auto'
    },
    animationContainer: {
        height: 100,
    }
});
